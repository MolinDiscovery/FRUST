# FRUST Cluster Submission

The cluster submission guide now lives in the MkDocs documentation site:

- `docs/cluster/submission.md`

Build or preview the docs with:

```bash
pip install -e ".[docs]"
mkdocs serve
```
